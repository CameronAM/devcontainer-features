{
  "name": "Mount AWS credentials",
  "id": "mount-aws-credentials",
  "version": "0.9.0",
  "description": "Mounts local .aws directory into container and adds configuration environment variables to use mounted credentials.",
  "mounts": [
    {
        "type": "bind",
        "source": "${localEnv:HOME}/.aws",
        "target": "/.aws"
    }
  ],
  "containerEnv": {
    "AWS_CONFIG_FILE": "/.aws/config",
    "AWS_SHARED_CREDENTIALS_FILE": "/.aws/credentials"
  }
}
